
1. 先下载数据：download_zhifu_data.py
2. 先 inite.db
3. 再生成缓存： precompute_ranks.py

注意：
.env TS_TOKEN 是 tushare 获取的



修改一下， web右侧 展示所选日期 前50日，后20日的走势图， 并且在所选日期 当天 标一下特殊标记，便于查看，带成交量，默认展示70天数据


┌─────────────────────────────────────────────────────────┐
│                    查询请求                              │
└─────────────────────────────────────────────────────────┘
                          ↓
┌─────────────────────────────────────────────────────────┐
│              ① 内存缓存 (最快)                           │  <0.01 秒
│         rank_memory_cache = {日期：排行数据}              │
└─────────────────────────────────────────────────────────┘
                          ↓ (未命中)
┌─────────────────────────────────────────────────────────┐
│              ② 磁盘缓存 (次快)                           │  <0.1 秒
│         rank_cache/{日期}.pkl                           │
└─────────────────────────────────────────────────────────┘
                          ↓ (未命中)
┌─────────────────────────────────────────────────────────┐
│              ③ SQLite 数据库查询                         │  2-5 秒
│         索引加速 + 批量查询                              │
└─────────────────────────────────────────────────────────┘
                          ↓ (首次)
┌─────────────────────────────────────────────────────────┐
│              ④ 预计算缓存 (可选)                         │  <0.1 秒
│         提前计算所有交易日排行                           │
└─────────────────────────────────────────────────────────┘


fupan/
├── stock_data_cache/          # 原始 CSV 数据
├── rank_cache/                # 排行缓存文件
├── templates/
│   └── index.html             # Web 前端
├── app.py                     # Flask 主程序 (优化版)
├── init_db.py                 # 数据库初始化脚本
├── precompute_ranks.py        # 预计算脚本
└── requirements.txt           # 依赖列表





帮我用python 写一个 中国股市 复盘系统， 具体要求如下：

1.首先下载所有股票历史数据保存的本地使用CSV文件保存(使用tushare 下载）
2.在web 界面上 有选择日期的功能， 然后在页面左侧展示 所选日期 当日的10日涨幅排行前50的股票。（使用flask) 
3. 在第2步的基础上，web页 右侧 显示 股票当时最近的20天走势图，带成交量； 每点击左侧的股票 右侧就要联动变换为左侧所选股票的数据。

修改一下， web右侧 展示所选日期 前20日，后10日的走势图， 并且在所选日期 当天 标一下特殊标记，便于查看，带成交量，默认展示30天数据


上边的都做的非常好， 现在需要 提高查询速度，请帮我出一个方案


目前股票数据都保存在 stock_data_cache 文件夹下 , 
需要写一个web 界面，具体功能如下：
1， 界面上有个日期选择功能，  选择日期后， 在页面左侧展示 所选日期 前一天涨停，当日没涨停的所有股票，
添加联动功能， 在选择左侧的股票后， 右侧要联动变换为所选股票 前20日，后10日的走势图， 并且在所选日期 当天 标一下特殊标记，便于查看，带成交量，默认展示30天数据

fupan_fanbao

修改一下逻辑：
在页面选择日期后， 左侧展示 所选日期 前一天涨停，当日没涨停，所有股票，把次日涨停的股票排前边，并且标记出来。 其他功能不变。

 现在查询非常慢，帮我用提前把数据计算好，放入到sqlite 
 

